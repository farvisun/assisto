# AGENTS.md

## Cursor Cloud specific instructions

This repository (`assisto`) is currently an empty/newly initialized project with only a `README.md` placeholder. There are no application services, dependencies, build systems, or test suites configured yet.

**Available system tools:** Node.js v22, npm v10, Python 3.12, git 2.43.

When application code is added, update this section with:
- How to install dependencies and run the dev server
- How to run lint, tests, and builds
- Any non-obvious startup caveats or environment requirements
